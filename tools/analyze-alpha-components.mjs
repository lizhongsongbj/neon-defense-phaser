import fs from 'node:fs/promises'; import path from 'node:path'; import sharp from 'sharp'
const root=process.cwd(), source={aerostat:'enemy-04-corporate-airship-fly.webp',devourer:'enemy-05-data-devourer-move.webp',faraday:'enemy-06-electromagnetic-riot-move.webp',hijacker:'enemy-07-hijack-hovercraft-fly.webp'}
for(const [id,file] of Object.entries(source)){const p=path.join(root,'public/assets/animations/enemies',file);const {data,info}=await sharp(p,{page:7}).ensureAlpha().raw().toBuffer({resolveWithObject:true});const w=info.width,h=info.height;const m=new Uint8Array(w*h);for(let i=0;i<w*h;i++)m[i]=data[i*info.channels+3]>18?1:0;const seen=new Uint8Array(w*h),comps=[];for(let y=0;y<h;y++)for(let x=0;x<w;x++){let k=y*w+x;if(!m[k]||seen[k])continue;let q=[k],n=0,minx=x,maxx=x,miny=y,maxy=y;seen[k]=1;while(q.length){let z=q.pop(),zx=z%w,zy=(z-zx)/w;n++;minx=Math.min(minx,zx);maxx=Math.max(maxx,zx);miny=Math.min(miny,zy);maxy=Math.max(maxy,zy);for(let dy=-1;dy<=1;dy++)for(let dx=-1;dx<=1;dx++){let nx=zx+dx,ny=zy+dy;if(nx<0||ny<0||nx>=w||ny>=h)continue;let ni=ny*w+nx;if(m[ni]&&!seen[ni]){seen[ni]=1;q.push(ni)}}}comps.push({n,minx,maxx,miny,maxy,w:maxx-minx+1,h:maxy-miny+1})}console.log(id,comps.sort((a,b)=>b.n-a.n).slice(0,8))}
