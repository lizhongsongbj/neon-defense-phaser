import { chromium } from 'playwright-core'
const browser=await chromium.launch({executablePath:'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',headless:true})
const page=await browser.newPage({viewport:{width:1280,height:800}});const errors=[]
page.on('pageerror',e=>errors.push(String(e)));page.on('response',r=>{if(r.status()>=400&&!r.url().endsWith('/favicon.ico'))errors.push(`${r.status()} ${r.url()}`)})
await page.goto('http://127.0.0.1:5174/',{waitUntil:'networkidle',timeout:30000});await page.click('#start-game');await page.waitForSelector('#deploy-mission:not([disabled])',{timeout:20000});await page.click('#deploy-mission');await page.waitForTimeout(800);await page.click('#intel-button');await page.waitForSelector('.intel-enemy-card');const cards=await page.locator('.intel-enemy-card').count();const text=await page.locator('#intel-content').innerText();const images=await page.locator('.intel-enemy-card img').evaluateAll(xs=>xs.map(x=>({src:x.getAttribute('src'),ok:x.complete&&x.naturalWidth>0})));await page.screenshot({path:'tools/smoke-shots/12-new-counter-enemies.png'});const expected=['电磁镇暴体','劫持浮游体','神经猎犬','育质母体','骨铠破障兽'];const namesOk=expected.every(name=>text.includes(name));const aerostatCard=page.locator('.intel-enemy-card').filter({hasText:'企业浮空艇'});const aerostatMechanical=(await aerostatCard.innerText()).includes('机械');console.log(JSON.stringify({cards,namesOk,aerostatMechanical,imagesOk:images.every(x=>x.ok),errors},null,2));await browser.close();if(cards!==12||!namesOk||!aerostatMechanical||!images.every(x=>x.ok)||errors.length)process.exit(1)
