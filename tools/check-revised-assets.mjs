import { PNG } from 'pngjs';import{readdir,readFile}from'node:fs/promises';const dir='public/assets/towers/new-concepts/revised';const files=(await readdir(dir)).filter(f=>f.endsWith('.png'));const out=[];for(const f of files){const p=PNG.sync.read(await readFile(`${dir}/${f}`));let a=0;for(let i=3;i<p.data.length;i+=4)if(p.data[i]===0)a++;out.push({f,w:p.width,h:p.height,a})}console.log(JSON.stringify(out,null,2));if(files.length!==10||out.some(x=>x.a===0))process.exit(1)
