import { chromium } from 'playwright-core'; import { writeFile, unlink } from 'node:fs/promises'; import { resolve } from 'node:path'
const dir='public/assets/enemies/new'; const files=['enemy-06-faraday-suppressor.jpg','enemy-07-hijack-hovercraft.jpg']
const browser=await chromium.launch({executablePath:'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',headless:true});const page=await browser.newPage();await page.goto('http://127.0.0.1:5174/',{waitUntil:'domcontentloaded'})
for(const file of files){const b64=await page.evaluate(async src=>{const im=new Image();im.src=src;await im.decode();const c=document.createElement('canvas');c.width=im.naturalWidth;c.height=im.naturalHeight;const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(im,0,0);const f=x.getImageData(0,0,c.width,c.height),{data,width,height}=f,v=new Uint8Array(width*height),q=new Int32Array(width*height);let h=0,t=0;const bg=i=>{const o=i*4,r=data[o],g=data[o+1],b=data[o+2],mx=Math.max(r,g,b),mn=Math.min(r,g,b);return mn>=200&&mx-mn<=28};const add=i=>{if(v[i]||!bg(i))return;v[i]=1;q[t++]=i};for(let a=0;a<width;a++){add(a);add((height-1)*width+a)}for(let y=0;y<height;y++){add(y*width);add(y*width+width-1)}while(h<t){const i=q[h++],a=i%width,y=Math.floor(i/width);if(a>0)add(i-1);if(a+1<width)add(i+1);if(y>0)add(i-width);if(y+1<height)add(i+width)}for(let i=0;i<v.length;i++)if(v[i])data[i*4+3]=0;x.putImageData(f,0,0);return c.toDataURL('image/png').split(',')[1]},`/assets/enemies/new/${file}`);const png=resolve(dir,file.replace('.jpg','.png'));await writeFile(png,Buffer.from(b64,'base64'));await unlink(resolve(dir,file));console.log(png)}await browser.close()
