import { chromium } from 'playwright-core'; import { readdir,writeFile } from 'node:fs/promises'; import { resolve } from 'node:path'
const root=resolve('public/assets/towers/new-concepts/revised'); const files=(await readdir(root)).filter(f=>f.endsWith('.png'))
const browser=await chromium.launch({executablePath:'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',headless:true}); const page=await browser.newPage(); await page.goto('http://127.0.0.1:5174/',{waitUntil:'domcontentloaded'})
for(const file of files){const base64=await page.evaluate(async(src)=>{const image=new Image();image.src=src;await image.decode();const canvas=document.createElement('canvas');canvas.width=image.naturalWidth;canvas.height=image.naturalHeight;const ctx=canvas.getContext('2d',{willReadFrequently:true});ctx.drawImage(image,0,0);const frame=ctx.getImageData(0,0,canvas.width,canvas.height),{data,width,height}=frame;const visited=new Uint8Array(width*height),queue=new Int32Array(width*height);let head=0,tail=0;const bg=i=>{const o=i*4,r=data[o],g=data[o+1],b=data[o+2],max=Math.max(r,g,b),min=Math.min(r,g,b);return min>=200&&max-min<=28};const add=i=>{if(visited[i]||!bg(i))return;visited[i]=1;queue[tail++]=i};for(let x=0;x<width;x++){add(x);add((height-1)*width+x)}for(let y=0;y<height;y++){add(y*width);add(y*width+width-1)}while(head<tail){const i=queue[head++],x=i%width,y=Math.floor(i/width);if(x>0)add(i-1);if(x+1<width)add(i+1);if(y>0)add(i-width);if(y+1<height)add(i+width)}for(let i=0;i<visited.length;i++)if(visited[i])data[i*4+3]=0;ctx.putImageData(frame,0,0);return canvas.toDataURL('image/png').split(',')[1]},`/assets/towers/new-concepts/revised/${file}?v=${Date.now()}`);await writeFile(resolve(root,file),Buffer.from(base64,'base64'));console.log(file)}await browser.close()
