const fs=require('fs'); const sharp=require('sharp');
(async()=>{const p='public/assets/animations/enemies/runtime-clean/faraday/move/frame-08.webp';const {data,info}=await sharp(p).ensureAlpha().raw().toBuffer({resolveWithObject:true});const w=info.width,h=info.height,m=new Uint8Array(w*h);for(let i=0;i<w*h;i++)m[i]=data[i*info.channels+3]>18?1:0;const core=new Uint8Array(w*h);for(let y=2;y<h-2;y++)for(let x=2;x<w-2;x++){let n=0;for(let dy=-2;dy<=2;dy++)for(let dx=-2;dx<=2;dx++)n+=m[(y+dy)*w+(x+dx)];if(n>=14)core[y*w+x]=1}const out=new Uint8Array(w*h);for(let y=0;y<h;y++)for(let x=0;x<w;x++){let keep=0;for(let dy=-6;dy<=6&&!keep;dy++)for(let dx=-6;dx<=6;dx++){const nx=x+dx,ny=y+dy;if(nx>=0&&ny>=0&&nx<w&&ny<h&&core[ny*w+nx]){keep=1;break}}out[y*w+x]=keep}for(let i=0;i<w*h;i++)if(!out[i])data[i*info.channels+3]=0;await sharp(data,{raw:info}).webp({lossless:true,quality:92}).toFile('tools/test-clean.webp')})()
